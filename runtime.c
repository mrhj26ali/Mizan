#include <stdio.h>
#include <stdlib.h>

// هذه الدالة سيستدعيها مترجمنا عند اكتشاف محاولة قسمة على صفر
void panic_div_zero() {
    printf("\n [خطأ وقت التشغيل]: محاولة قسمة على صفر! تم إيقاف البرنامج بأمان \n");
    exit(1); 
}